Email replacer should import a csv file (import.csv) of emails from the @test.chris.uk domain and covert them to @result.chris.uk
It creates & reads from a registry so that it continues from where left off if you input more addresses to the .csv input. 
